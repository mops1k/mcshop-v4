{% extends ':Default:base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block content %}
    <h4>{{ 'news.comments.edit_title'|trans }}</h4>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">{{ 'news.comments.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
