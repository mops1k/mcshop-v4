{% extends ':Default:base.html.twig' %}
{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block content %}
    <h4>{{ header_title|raw }}</h4>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button type="submit" class="btn btn-success">{{ 'news.submit'|trans }}</button>
    {{ form_end(form) }}
{% endblock %}
